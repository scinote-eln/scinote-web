<%= bootstrap_form_for org, url: update_organization_path(org, format: :json), remote: :true, method: :put do |f| %>
  <%= f.text_field :name, label: t("users.settings.organizations.edit.name_label") %>
<% end %>