<% if can_read_repository?(repository) %>
  <%= route_to_other_team repository_path(id: repository.id),
                          repository.team,
                          repository.name %>
<% else %>
  <%= repository.name %>
<% end %>
